# Browser build: keep empty in production so requests remain same-origin.
# VITE_API_BASE_URL=

# Server-only variables. Never prefix these with VITE_.
BACKEND_API_URL=https://bybit-intraday-trading-bot.onrender.com
BACKEND_ADMIN_TOKEN=replace-with-backend-admin-token
FRONTEND_CONTROL_TOKEN=replace-with-long-random-operator-token

# Bybit credentials belong only in the backend environment.
# Never place BYBIT_API_KEY or BYBIT_API_SECRET in this frontend project.
